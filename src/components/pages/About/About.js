// @flow
import * as React from 'react';
import styled from 'styled-components';

const P = styled.p`
  color: 'gray';
`;

export const About = () => (
  <React.Fragment>
    <P>This is about page</P>
  </React.Fragment>
);
